import React from "react";

interface Props {
    className?: string;
}

export const StudentsList: React.FC<Props> = ({  }) => {
    return (
        <div className="flex">
            
        </div>
    );
};